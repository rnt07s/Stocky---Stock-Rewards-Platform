# Stocky System Architecture - Visual Guide

## 📐 System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT (Postman/cURL)                    │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP Requests
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GIN WEB SERVER (Port 8080)                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      MIDDLEWARE STACK                     │   │
│  │  • Logging (request/response/latency)                     │   │
│  │  • Recovery (panic handler)                               │   │
│  │  • CORS (cross-origin)                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                         HANDLERS LAYER                           │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │POST /reward │GET /today-  │GET /stats   │GET /portfolio│    │
│  │             │stocks       │             │             │     │
│  └─────┬───────┴─────┬───────┴─────┬───────┴─────┬───────┘     │
└────────┼─────────────┼─────────────┼─────────────┼─────────────┘
         │             │             │             │
         ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICES LAYER                              │
│  ┌───────────────────────────┐  ┌──────────────────────────┐   │
│  │   RewardService           │  │  StockPriceService       │   │
│  │  • CreateReward()         │  │  • GetCurrentPrice()     │   │
│  │  • GetTodayStocks()       │  │  • UpdateAllPrices()     │   │
│  │  • GetUserStats()         │  │  • StartPriceUpdater()   │   │
│  │  • GetUserPortfolio()     │  │    (Hourly Goroutine)    │   │
│  │  • Calculate Fees         │  │                          │   │
│  │  • Update Holdings        │  └──────────────────────────┘   │
│  └───────────────────────────┘                                  │
└────────────────┬───────────────────────────┬────────────────────┘
                 │                           │
                 ▼                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REPOSITORY LAYER                              │
│  ┌──────────────┬──────────────┬──────────────┐                │
│  │ RewardRepo   │ StockRepo    │ LedgerRepo   │                │
│  │ • Create     │ • GetPrice   │ • Create     │                │
│  │ • GetByKey   │ • GetStock   │   Entries    │                │
│  │ • GetToday   │ • SavePrice  │              │                │
│  └──────────────┴──────────────┴──────────────┘                │
└────────────────┬───────────────────────────┬────────────────────┘
                 │                           │
                 ▼                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                     POSTGRESQL DATABASE                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Tables:                                                  │   │
│  │  • users                 • user_holdings                  │   │
│  │  • stocks                • stock_prices                   │   │
│  │  • reward_events         • ledger_entries                 │   │
│  │  • stock_events                                           │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example: Creating a Reward

```
1. CLIENT sends POST /api/v1/reward
   ↓
   Body: {
     "idempotency_key": "reward-alice-001",
     "user_id": "alice",
     "stock_symbol": "TCS",
     "shares_quantity": 2.5
   }

2. GIN ROUTER receives request
   ↓ Middleware chain processes request

3. HANDLER validates input
   ↓ Calls RewardService.CreateReward()

4. SERVICE LAYER logic:
   ├─ Check idempotency (RewardRepo.GetByKey)
   │   └─ If exists: return cached event ✅
   ├─ Validate stock exists (StockRepo.GetStock)
   ├─ Get current price (PriceService.GetCurrentPrice)
   ├─ Calculate fees
   │   ├─ Brokerage: 0.05% of value
   │   ├─ STT: 0.25% of value
   │   ├─ GST: 18% of brokerage
   │   ├─ Exchange: 0.03% of value
   │   └─ SEBI: 0.01% of value
   ├─ Create reward_event (RewardRepo.Create)
   ├─ Update user_holdings (RewardRepo.Upsert)
   └─ Create ledger_entries (LedgerRepo.Create)
        ├─ Debit: Stock Inventory ₹8,750
        ├─ Credit: Cash ₹8,750
        ├─ Debit: Fees ₹30.72
        └─ Credit: Cash ₹30.72

5. REPOSITORY LAYER executes SQL
   ↓
   INSERT INTO reward_events (...)
   INSERT INTO ledger_entries (...)
   UPDATE user_holdings SET ...

6. DATABASE commits transaction
   ↓ Returns created records

7. HANDLER sends response to client
   ↓
   {
     "success": true,
     "data": {
       "id": 1,
       "total_value": 8750.00,
       "total_fees": 30.72,
       "total_cost": 8780.72
     }
   }
```

---

## 💾 Database Relationships

```
┌─────────────┐
│   users     │
│─────────────│         ┌──────────────────┐
│ id          │◄────────│  reward_events   │
│ user_id (PK)│  1:N    │──────────────────│
│ name        │         │ id               │
│ email       │         │ idempotency_key  │
└─────────────┘         │ user_id (FK)     │
                        │ stock_symbol (FK)│
                        │ shares_quantity  │
    ┌─────────────┐     │ price_per_share  │
    │   stocks    │     │ total_value      │
    │─────────────│     │ total_fees       │
    │ id          │◄────│ rewarded_at      │
    │ symbol (PK) │  1:N└──────────────────┘
    │ company_name│                │
    │ is_active   │                │ 1:N
    └──────┬──────┘                ▼
           │               ┌──────────────────┐
           │ 1:N           │ ledger_entries   │
           │               │──────────────────│
           ▼               │ id               │
    ┌──────────────┐      │ entry_group_id   │
    │stock_prices  │      │ reward_event_id  │
    │──────────────│      │ account_type     │
    │ id           │      │ debit_amount     │
    │ stock_symbol │      │ credit_amount    │
    │ price        │      └──────────────────┘
    │ timestamp    │
    └──────────────┘
           │
           │ 1:N
           ▼
    ┌──────────────┐
    │user_holdings │
    │──────────────│
    │ id           │
    │ user_id (FK) │
    │ stock_symbol │
    │ total_shares │
    │ avg_price    │
    └──────────────┘
```

---

## 🕒 Background Job: Stock Price Updater

```
┌───────────────────────────────────────────────────────────┐
│               PRICE UPDATER GOROUTINE                     │
│                                                            │
│  Start: Server initialization                             │
│  Interval: 60 minutes (configurable)                      │
│                                                            │
│  ┌─────────────────────────────────────────────────┐     │
│  │  Loop every 60 minutes:                         │     │
│  │                                                  │     │
│  │  1. Get list of active stocks                   │     │
│  │     └─ ["TCS", "INFY", "RELIANCE", ...]         │     │
│  │                                                  │     │
│  │  2. For each stock:                             │     │
│  │     ├─ Generate mock price                      │     │
│  │     │  (base_price ± random 5%)                 │     │
│  │     │                                            │     │
│  │     └─ Save to stock_prices table               │     │
│  │        INSERT INTO stock_prices (...)           │     │
│  │                                                  │     │
│  │  3. Log completion                              │     │
│  │     └─ "Updated 10 prices at 14:00:00"          │     │
│  │                                                  │     │
│  └─────────────────────────────────────────────────┘     │
│                                                            │
│  Runs independently, doesn't block API requests          │
└───────────────────────────────────────────────────────────┘

Example prices generated:
┌──────────┬────────────┬─────────────┐
│ Stock    │ Base Price │ Mock Price  │
├──────────┼────────────┼─────────────┤
│ TCS      │ ₹3,500     │ ₹3,521.45   │
│ INFY     │ ₹1,500     │ ₹1,487.20   │
│ RELIANCE │ ₹2,500     │ ₹2,548.90   │
└──────────┴────────────┴─────────────┘
```

---

## 💰 Fee Calculation Breakdown

```
User receives 2.5 shares of TCS @ ₹3,521.45

┌─────────────────────────────────────────────────┐
│               FEE CALCULATION                   │
└─────────────────────────────────────────────────┘

Base Value:
  2.5 shares × ₹3,521.45 = ₹8,803.625
  Rounded to: ₹8,803.63

Fees (calculated on total value):
  ├─ Brokerage (0.05%)
  │  └─ ₹8,803.63 × 0.0005 = ₹4.40
  │
  ├─ STT (0.25%)
  │  └─ ₹8,803.63 × 0.0025 = ₹22.01
  │
  ├─ GST (18% of brokerage)
  │  └─ ₹4.40 × 0.18 = ₹0.79
  │
  ├─ Exchange Fee (0.03%)
  │  └─ ₹8,803.63 × 0.0003 = ₹2.64
  │
  └─ SEBI Fee (0.01%)
     └─ ₹8,803.63 × 0.0001 = ₹0.88

Total Fees: ₹30.72

TOTAL COST TO STOCKY:
  ₹8,803.63 (shares) + ₹30.72 (fees) = ₹8,834.35

USER RECEIVES:
  2.5 shares of TCS (full amount, no deductions!)
```

---

## 📊 Ledger Entry Visualization

```
Double-Entry Bookkeeping for Reward Event #1

┌─────────────────────────────────────────────────────┐
│         DEBIT (Left Side)    │   CREDIT (Right Side) │
├──────────────────────────────┼──────────────────────┤
│ Stock Inventory (TCS)        │                      │
│   ₹8,803.63                  │                      │
│                              │   Cash Account       │
│                              │     ₹8,803.63        │
├──────────────────────────────┼──────────────────────┤
│ Fees Expense                 │                      │
│   ₹30.72                     │                      │
│                              │   Cash Account       │
│                              │     ₹30.72           │
├──────────────────────────────┼──────────────────────┤
│ TOTAL: ₹8,834.35             │ TOTAL: ₹8,834.35     │
└──────────────────────────────┴──────────────────────┘
                    ✅ Balanced!

In database (ledger_entries table):
┌──────┬─────────────────┬────────┬────────┬────────┐
│ ID   │ Account         │ Symbol │ Debit  │ Credit │
├──────┼─────────────────┼────────┼────────┼────────┤
│ 1    │ stock_inventory │ TCS    │ 8803.63│   0    │
│ 2    │ cash_outflow    │ NULL   │   0    │ 8803.63│
│ 3    │ fees_expense    │ NULL   │  30.72 │   0    │
│ 4    │ cash_outflow    │ NULL   │   0    │  30.72 │
└──────┴─────────────────┴────────┴────────┴────────┘
```

---

## 🔐 Idempotency Flow

```
First Request (idempotency_key = "abc123"):
┌─────────────────────────────────────────────┐
│ 1. Check: Does "abc123" exist?              │
│    └─ Query: SELECT * WHERE key = 'abc123'  │
│       Result: NOT FOUND                     │
│                                              │
│ 2. Create new reward event                  │
│    └─ INSERT INTO reward_events (...)       │
│       Returns: Event ID = 1                 │
│                                              │
│ 3. Return: HTTP 201 Created                 │
│    {                                         │
│      "id": 1,                                │
│      "idempotency_key": "abc123",           │
│      ...                                     │
│    }                                         │
└─────────────────────────────────────────────┘

Second Request (SAME key = "abc123"):
┌─────────────────────────────────────────────┐
│ 1. Check: Does "abc123" exist?              │
│    └─ Query: SELECT * WHERE key = 'abc123'  │
│       Result: FOUND (Event ID = 1) ✅        │
│                                              │
│ 2. Skip creation, return cached event       │
│                                              │
│ 3. Return: HTTP 201 Created (same response) │
│    {                                         │
│      "id": 1,                                │
│      "idempotency_key": "abc123",           │
│      ...                                     │
│    }                                         │
│                                              │
│ ⚠️  NO DUPLICATE IN DATABASE                 │
└─────────────────────────────────────────────┘
```

---

## 🎯 Scaling Architecture

```
                        ┌──────────────┐
                        │ Load Balancer│
                        │   (Nginx)    │
                        └──────┬───────┘
                               │
            ┌──────────────────┼──────────────────┐
            ▼                  ▼                  ▼
    ┌───────────────┐  ┌───────────────┐  ┌───────────────┐
    │ API Server 1  │  │ API Server 2  │  │ API Server 3  │
    │   (Stateless) │  │   (Stateless) │  │   (Stateless) │
    └───────┬───────┘  └───────┬───────┘  └───────┬───────┘
            │                  │                  │
            └──────────────────┼──────────────────┘
                               ▼
                    ┌────────────────────┐
                    │   Redis Cache      │
                    │ • Stock prices     │
                    │ • User portfolios  │
                    │   (5 min TTL)      │
                    └────────┬───────────┘
                             │
            ┌────────────────┼────────────────┐
            ▼                ▼                ▼
    ┌──────────────┐  ┌─────────────┐  ┌──────────────┐
    │ PostgreSQL   │  │ PostgreSQL  │  │ PostgreSQL   │
    │   Primary    │──│  Replica 1  │  │  Replica 2   │
    │  (Writes)    │  │  (Reads)    │  │  (Reads)     │
    └──────────────┘  └─────────────┘  └──────────────┘

Benefits:
• Horizontal scaling (add more API servers)
• High availability (no single point of failure)
• Read replicas reduce primary DB load
• Cache reduces database queries by 80%+
```

---

## 📈 Performance Optimization Strategy

```
┌──────────────────────────────────────────────────────┐
│                 PERFORMANCE LAYERS                   │
└──────────────────────────────────────────────────────┘

Layer 1: Application (Go)
├─ Connection pooling (25 max connections)
├─ Efficient queries (SELECT only needed columns)
├─ Batch operations (insert multiple ledger entries)
└─ Goroutines for background jobs

Layer 2: Database (PostgreSQL)
├─ Indexes on frequently queried columns
│  └─ reward_events(user_id, rewarded_at)
├─ Partitioning (split table by month)
│  └─ reward_events_2025_01, reward_events_2025_02
└─ Materialized views for analytics

Layer 3: Caching (Redis - Future)
├─ Stock prices (TTL: 5 min)
├─ User portfolios (TTL: 10 min, invalidate on new reward)
└─ Aggregated stats (TTL: 1 hour)

Layer 4: CDN (Future - for static content)
└─ API documentation, images

Expected Improvements:
• Cached reads: 10x faster (20ms → 2ms)
• Indexed queries: 100x faster (1000ms → 10ms)
• Partitioning: Maintains performance as data grows
```

---

## 🧪 Testing Strategy

```
Unit Tests (Repository Layer):
┌─────────────────────────────────────┐
│ • Test CreateRewardEvent()          │
│ • Test GetByIdempotencyKey()        │
│ • Test fee calculations             │
│ • Mock database responses           │
└─────────────────────────────────────┘

Integration Tests (Service Layer):
┌─────────────────────────────────────┐
│ • Full flow: Create reward          │
│ • Verify user_holdings updated      │
│ • Verify ledger_entries created     │
│ • Test idempotency                  │
└─────────────────────────────────────┘

API Tests (End-to-End):
┌─────────────────────────────────────┐
│ • POST /reward → GET /portfolio     │
│ • Test duplicate requests           │
│ • Test invalid inputs               │
│ • Test stock not found              │
└─────────────────────────────────────┘

Load Tests (Performance):
┌─────────────────────────────────────┐
│ • 1000 concurrent users             │
│ • Measure: throughput, latency      │
│ • Target: p95 < 200ms               │
└─────────────────────────────────────┘
```

---

This visual guide provides a comprehensive overview of how all components work together in the Stocky system!
